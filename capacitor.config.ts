import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'share and save plugin',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
